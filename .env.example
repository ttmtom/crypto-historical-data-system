# Database connection settings
# These variables are used by both the Spring application and docker-compose.
# Copy this file to .env and fill in your local configuration.
DB_NAME=postgres
DB_USERNAME=timescaledb
DB_PASSWORD=password